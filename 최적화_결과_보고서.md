# 🎮 Unity 3D FPS Game 최적화 결과 보고서

## 📋 프로젝트 정보
- **프로젝트명**: 3D FPS Game
- **Unity 버전**: 6000.0.60f1
- **렌더 파이프라인**: URP (Universal Render Pipeline)
- **작성일**: 2024년 12월
- **작성자**: [이름]

---

# 📊 Part 1: Before (최적화 전 상태)

## 1.1 성능 측정 결과

### 인게임 스크린샷
> [여기에 최적화 전 인게임 스크린샷 삽입]

### Statistics 창 (Game 뷰 → Stats)
> [여기에 Stats 창 스크린샷 삽입]

| 항목 | 최적화 전 수치 |
|------|---------------|
| FPS | ___ fps |
| Batches | ___ |
| SetPass calls | ___ |
| Tris (삼각형) | ___ |
| Verts (정점) | ___ |
| Shadow casters | ___ |
| Screen | ___x___ |

### Frame Debugger 스크린샷
> [여기에 Frame Debugger 스크린샷 삽입]
> 
> **확인 방법**: Window → Analysis → Frame Debugger → Enable

### Profiler 스크린샷
> [여기에 Profiler 스크린샷 삽입]
> 
> **확인 방법**: Window → Analysis → Profiler

| 항목 | 최적화 전 수치 |
|------|---------------|
| CPU Main Thread | ___ ms |
| GPU | ___ ms |
| Rendering | ___ ms |
| Scripts | ___ ms |
| Physics | ___ ms |

---

# 🔧 Part 2: 최적화 적용 내용

## 2.1 조명 최적화 (Lighting Optimization)

### 2.1.1 조명 모드 비교

| 모드 | 특징 | 적용 대상 | 성능 |
|------|------|----------|------|
| **Realtime** | 실시간 계산, 동적 그림자 | 움직이는 광원 | 무거움 |
| **Baked** | 미리 계산, 정적 그림자 | 고정된 광원/오브젝트 | 가벼움 |
| **Mixed** | 실시간 + Baked 혼합 | 대부분의 상황 | 중간 |

### 2.1.2 Baked Lighting 설정 방법

#### Step 1: 오브젝트 Static 설정
```
1. Hierarchy에서 고정된 오브젝트(건물, 지형, 벽 등) 선택
2. Inspector 상단 → Static 드롭다운 클릭
3. "Contribute Global Illumination" 체크 ✅
4. "Reflection Probe Static" 체크 ✅
```

#### Step 2: 렌더러 Lightmap 설정
```
1. Static 오브젝트의 MeshRenderer 컴포넌트 확인
2. Lighting 섹션 확장
3. "Contribute Global Illumination" 체크 ✅
4. "Receive Global Illumination" → Lightmaps 선택
```

#### Step 3: 3D 모델 Lightmap UV 생성
```
1. Project 창에서 3D 모델 파일(.fbx) 선택
2. Inspector → Model 탭
3. "Generate Lightmap UVs" 체크 ✅
4. Apply 클릭
```

#### Step 4: Lighting 베이크
```
1. Window → Rendering → Lighting
2. Scene 탭 선택
3. Mixed Lighting 섹션:
   - Lighting Mode 선택:
     * Subtractive (저사양) - 간접광 없음, 가장 가벼움
     * Shadowmask (고사양) - 고품질 그림자, 무거움
     * Baked Indirect (중사양, 권장) - 균형잡힌 품질
4. "Generate Lighting" 클릭
```

### 2.1.3 적용 결과
> [Baked Lighting 적용 전/후 스크린샷 삽입]

| 항목 | Before | After | 개선율 |
|------|--------|-------|--------|
| Realtime Lights | ___ | ___ | ___% |
| Shadow casters | ___ | ___ | ___% |
| Lighting 계산 시간 | ___ ms | ___ ms | ___% |

---

## 2.2 Light Probe Group 설정

### 2.2.1 Light Probe란?
```
📚 비유: "조명 샘플링 포인트"

Baked Lighting은 Static 오브젝트에만 적용됩니다.
그렇다면 움직이는 캐릭터나 적은 어떻게 조명을 받을까요?

Light Probe는 공간의 특정 지점에서 조명 정보를 미리 저장해두고,
동적 오브젝트가 그 근처를 지나갈 때 해당 조명 정보를 사용합니다.

마치 "여기는 밝아", "여기는 어두워" 라고 적어둔 메모지 같습니다!
```

### 2.2.2 설정 방법
```
1. Hierarchy → Create → Light → Light Probe Group
2. 씬에서 플레이어가 이동하는 영역에 배치
3. Edit Light Probes 버튼 클릭
4. 프로브 추가/이동/삭제로 밀도 조절
   - 조명 변화가 큰 곳: 밀도 높게
   - 균일한 조명 영역: 밀도 낮게
5. Lighting 창에서 다시 Generate Lighting
```

### 2.2.3 적용 결과
> [Light Probe 적용 전/후 동적 오브젝트 스크린샷 삽입]

---

## 2.3 Reflection Probe 설정

### 2.3.1 Reflection Probe란?
```
📚 비유: "360도 파노라마 사진"

금속이나 유리 재질은 주변 환경을 반사합니다.
실시간으로 반사를 계산하면 매우 무겁습니다.

Reflection Probe는 특정 위치에서 360도 환경을 캡처해두고,
반사 재질이 이 이미지를 사용해서 반사 효과를 냅니다.

마치 방 안에서 360도 사진을 찍어두고,
거울에 그 사진을 보여주는 것과 같습니다!
```

### 2.3.2 설정 방법
```
1. Hierarchy → Create → Light → Reflection Probe
2. 반사가 필요한 영역(실내, 금속 오브젝트 주변)에 배치
3. Inspector 설정:
   - Type: Baked (권장) 또는 Realtime
   - Resolution: 128 또는 256 (높을수록 품질↑, 성능↓)
   - Box Projection: 실내 공간에서 체크 ✅
4. Bake 버튼 클릭
```

### 2.3.3 적용 결과
> [Reflection Probe 적용 전/후 반사 재질 스크린샷 삽입]

---

## 2.4 Adaptive Probe Volume (APV) - Unity 6 신기능

### 2.4.1 APV란?
```
📚 비유: "자동 Light Probe 생성기"

기존 Light Probe Group은 수동으로 배치해야 했습니다.
APV는 씬을 분석해서 자동으로 최적의 위치에 프로브를 배치합니다!

Unity 6의 새로운 기능으로, 대규모 씬에서 특히 유용합니다.
```

### 2.4.2 설정 방법 (URP)
```
1. Edit → Project Settings → Graphics
2. URP Asset 선택
3. Lighting 섹션:
   - Light Probe System: Adaptive Probe Volumes 선택
4. Hierarchy → Create → Light → Adaptive Probe Volume
5. 영역 크기 조절
6. Lighting 창에서 Generate Lighting
```

---

## 2.5 용량 최적화 (Asset Optimization)

### 2.5.1 텍스처 압축
```
1. Project 창에서 텍스처 선택
2. Inspector → Default 탭:
   - Max Size: 원본보다 작게 (2048 → 1024 또는 512)
   - Compression: Normal Quality 또는 High Quality
   - Format: Automatic (플랫폼별 최적 포맷)
3. Apply 클릭
```

### 2.5.2 모델 최적화
```
1. Project 창에서 FBX 모델 선택
2. Inspector → Model 탭:
   - Mesh Compression: Medium 또는 High
   - Read/Write Enabled: 체크 해제 ❌ (런타임 수정 불필요 시)
   - Optimize Mesh: Everything
3. Apply 클릭
```

### 2.5.3 오디오 압축
```
1. Project 창에서 오디오 파일 선택
2. Inspector:
   - Load Type: Compressed In Memory (짧은 효과음)
                Streaming (긴 BGM)
   - Compression Format: Vorbis (품질), ADPCM (성능)
   - Quality: 70-80% 권장
3. Apply 클릭
```

### 2.5.4 적용 결과

| 항목 | Before | After | 절감량 |
|------|--------|-------|--------|
| 빌드 용량 | ___ MB | ___ MB | ___ MB |
| 텍스처 메모리 | ___ MB | ___ MB | ___ MB |
| 오디오 메모리 | ___ MB | ___ MB | ___ MB |

---

## 2.6 메모리 최적화 (Memory Optimization)

### 2.6.1 오브젝트 풀링 확인
```
📚 비유: "재사용 가능한 컵"

총알, 이펙트 등을 매번 Instantiate/Destroy 하면
가비지 컬렉션(GC)이 자주 발생해서 끊김 현상이 생깁니다.

오브젝트 풀링은 미리 오브젝트를 만들어두고 재사용합니다.
일회용 컵 대신 씻어서 재사용하는 것과 같습니다!
```

### 2.6.2 Resources 폴더 정리
```
⚠️ Resources 폴더의 모든 에셋은 게임 시작 시 메모리에 로드됩니다!

권장사항:
- 자주 사용하는 에셋만 Resources에 보관
- 나머지는 Addressables 또는 직접 참조 사용
```

### 2.6.3 적용 결과
> [Profiler Memory 탭 스크린샷 삽입]

| 항목 | Before | After | 개선율 |
|------|--------|-------|--------|
| Total Memory | ___ MB | ___ MB | ___% |
| Textures | ___ MB | ___ MB | ___% |
| Meshes | ___ MB | ___ MB | ___% |
| Audio | ___ MB | ___ MB | ___% |

---

## 2.7 CPU 최적화 (CPU Optimization)

### 2.7.1 스크립트 최적화
```csharp
// ❌ 나쁜 예: 매 프레임 Find 호출
void Update()
{
    GameObject player = GameObject.Find("Player"); // 매우 무거움!
    player.transform.position = newPos;
}

// ✅ 좋은 예: 캐싱 사용
private Transform _playerTransform;

void Start()
{
    _playerTransform = GameObject.Find("Player").transform; // 한 번만!
}

void Update()
{
    _playerTransform.position = newPos; // 캐시된 참조 사용
}
```

### 2.7.2 Physics 최적화
```
Edit → Project Settings → Physics:
- Default Solver Iterations: 6 → 4 (정밀도 ↓, 성능 ↑)
- Auto Simulation: 필요 없으면 체크 해제
- Layer Collision Matrix: 불필요한 충돌 체크 해제
```

### 2.7.3 적용 결과
> [Profiler CPU 탭 스크린샷 삽입]

| 항목 | Before | After | 개선율 |
|------|--------|-------|--------|
| Scripts | ___ ms | ___ ms | ___% |
| Physics | ___ ms | ___ ms | ___% |
| Animation | ___ ms | ___ ms | ___% |

---

## 2.8 GPU 최적화 (GPU Optimization)

### 2.8.1 Occlusion Culling (차폐 컬링)
```
📚 비유: "보이지 않는 건 그리지 않기"

카메라에 보이지 않는 오브젝트(벽 뒤, 화면 밖)는 
렌더링할 필요가 없습니다.

Occlusion Culling은 보이지 않는 오브젝트를 자동으로 제외합니다.
```

**설정 방법:**
```
1. 차폐물(벽, 큰 건물) 선택
2. Inspector → Static → Occluder Static 체크 ✅
3. 차폐될 오브젝트 선택
4. Inspector → Static → Occludee Static 체크 ✅
5. Window → Rendering → Occlusion Culling
6. Bake 탭 → Bake 클릭
```

### 2.8.2 Draw Call Batching (배칭)
```
📚 비유: "택배 묶음 배송"

GPU에게 "이거 그려", "저거 그려" 하나씩 명령하면 느립니다.
비슷한 오브젝트를 묶어서 "이거 다 그려!" 하면 빠릅니다.
```

**Static Batching:**
```
1. 움직이지 않는 오브젝트 선택
2. Inspector → Static → Batching Static 체크 ✅
3. Player Settings → Other Settings → Static Batching 체크 ✅
```

**GPU Instancing:**
```
1. 같은 메시를 여러 개 사용하는 경우 (나무, 풀 등)
2. Material 선택 → Inspector
3. "Enable GPU Instancing" 체크 ✅
```

**SRP Batcher (URP):**
```
1. Project Settings → Graphics → URP Asset
2. SRP Batcher: Enabled ✅
```

### 2.8.3 LOD (Level of Detail)
```
📚 비유: "멀면 대충, 가까우면 정밀하게"

멀리 있는 오브젝트는 어차피 작게 보이므로
저폴리곤 모델로 대체해도 눈에 띄지 않습니다.
```

**설정 방법:**
```
1. 오브젝트에 LOD Group 컴포넌트 추가
2. LOD 0: 원본 모델 (가까울 때)
3. LOD 1: 중간 폴리곤 (중간 거리)
4. LOD 2: 저폴리곤 (멀 때)
5. Culled: 매우 멀면 안 그림
```

### 2.8.4 적용 결과
> [Frame Debugger 스크린샷 삽입]

| 항목 | Before | After | 개선율 |
|------|--------|-------|--------|
| Batches | ___ | ___ | ___% |
| SetPass calls | ___ | ___ | ___% |
| Triangles | ___ | ___ | ___% |
| Vertices | ___ | ___ | ___% |

---

## 2.9 포스트 프로세싱 (Post Processing)

### 2.9.1 포스트 프로세싱이란?
```
📚 비유: "사진 필터/뽀샵"

게임 화면이 렌더링된 후에 추가로 효과를 적용합니다.
블룸(빛 번짐), 색보정, 비네트 등으로 분위기를 연출합니다.
```

### 2.9.2 URP 포스트 프로세싱 설정
```
1. Hierarchy → Volume → Global Volume 생성
2. Volume 컴포넌트 → Profile → New 클릭
3. Add Override로 효과 추가:
   - Bloom: 빛 번짐 효과
   - Color Adjustments: 색보정
   - Vignette: 화면 가장자리 어둡게
   - Tonemapping: HDR → LDR 변환
   - Ambient Occlusion: 틈새 그림자 (성능 주의!)
```

### 2.9.3 포스트 프로세싱 성능 영향
```
⚠️ 성능 주의 효과:
- Ambient Occlusion: GPU 부하 높음
- Motion Blur: GPU 부하 높음
- Depth of Field: GPU 부하 높음

✅ 가벼운 효과:
- Bloom
- Color Adjustments
- Vignette
- Tonemapping
```

### 2.9.4 적용 결과
> [포스트 프로세싱 전/후 스크린샷 삽입]

---

# 📊 Part 3: After (최적화 후 상태)

## 3.1 최종 성능 측정 결과

### 인게임 스크린샷
> [여기에 최적화 후 인게임 스크린샷 삽입]

### Statistics 창
> [여기에 최적화 후 Stats 창 스크린샷 삽입]

### Frame Debugger 스크린샷
> [여기에 최적화 후 Frame Debugger 스크린샷 삽입]

### Profiler 스크린샷
> [여기에 최적화 후 Profiler 스크린샷 삽입]

---

# 📈 Part 4: 최종 비교 분석

## 4.1 성능 개선 요약

| 카테고리 | 항목 | Before | After | 개선율 |
|----------|------|--------|-------|--------|
| **FPS** | 평균 FPS | ___ | ___ | ___% ↑ |
| **렌더링** | Batches | ___ | ___ | ___% ↓ |
| | SetPass calls | ___ | ___ | ___% ↓ |
| | Triangles | ___ | ___ | ___% ↓ |
| **CPU** | Main Thread | ___ ms | ___ ms | ___% ↓ |
| | Scripts | ___ ms | ___ ms | ___% ↓ |
| **GPU** | Rendering | ___ ms | ___ ms | ___% ↓ |
| **메모리** | Total | ___ MB | ___ MB | ___% ↓ |
| **용량** | Build Size | ___ MB | ___ MB | ___% ↓ |

## 4.2 적용한 최적화 체크리스트

| 최적화 항목 | 적용 여부 | 비고 |
|------------|----------|------|
| Baked Lighting | ☐ | |
| Light Probe Group | ☐ | |
| Reflection Probe | ☐ | |
| Occlusion Culling | ☐ | |
| Static Batching | ☐ | |
| GPU Instancing | ☐ | |
| SRP Batcher | ☐ | |
| LOD 설정 | ☐ | |
| 텍스처 압축 | ☐ | |
| 모델 최적화 | ☐ | |
| 오디오 압축 | ☐ | |
| 포스트 프로세싱 | ☐ | |

---

# 🔍 Part 5: Frame Debugger 분석 방법

## 5.1 Frame Debugger 열기
```
1. Window → Analysis → Frame Debugger
2. Enable 버튼 클릭
3. 게임 실행 중 일시정지하면 현재 프레임 분석 가능
```

## 5.2 주요 확인 항목

### Draw Call 분석
```
좌측 트리에서 각 Draw Call 확인:
- RenderForward.RenderLoopJob: 오브젝트 렌더링
- Shadows.RenderShadowMap: 그림자 렌더링
- PostProcess: 포스트 프로세싱

⚠️ 같은 머티리얼인데 Batch되지 않으면:
- Static 설정 확인
- 머티리얼 인스턴싱 확인
- SRP Batcher 호환성 확인
```

### Batch 끊김 원인 확인
```
Draw Call 선택 → 우측 패널에서 "Why this draw call can't be batched" 확인
- Different materials
- Different mesh
- Not in the same static batch
```

---

# 📚 Part 6: 학습 내용 정리

## 6.1 조명 모드 비교표

| 모드 | 실시간 직접광 | 실시간 간접광 | Baked 직접광 | Baked 간접광 | 권장 상황 |
|------|-------------|-------------|-------------|-------------|----------|
| Realtime | ✅ | ✅ | ❌ | ❌ | 완전 동적 씬 |
| Baked | ❌ | ❌ | ✅ | ✅ | 완전 정적 씬 |
| Mixed - Subtractive | ✅ | ❌ | ❌ | ✅ | 저사양 |
| Mixed - Baked Indirect | ✅ | ❌ | ❌ | ✅ | 중사양 (권장) |
| Mixed - Shadowmask | ✅ | ❌ | ✅ | ✅ | 고사양 |

## 6.2 최적화 우선순위 (영향도 순)

1. **Lighting Bake** - 가장 큰 성능 개선
2. **Occlusion Culling** - 복잡한 씬에서 효과적
3. **Batching** - Draw Call 감소
4. **텍스처 압축** - 메모리/용량 감소
5. **LOD 설정** - 오픈월드에서 효과적
6. **스크립트 최적화** - GC 스파이크 방지

---

# ✅ 결론

## 주요 성과
1. FPS가 ___에서 ___로 ___% 향상되었습니다.
2. Batches가 ___에서 ___로 ___% 감소했습니다.
3. 메모리 사용량이 ___MB에서 ___MB로 ___% 절감되었습니다.

## 추가 개선 가능 사항
- [ ] Addressables 시스템 도입
- [ ] LOD 그룹 추가 설정
- [ ] 쉐이더 최적화
- [ ] 오브젝트 풀링 확장

---

> 📝 **참고자료**
> - Unity 공식 문서: [Optimizing Graphics Performance](https://docs.unity3d.com/Manual/OptimizingGraphicsPerformance.html)
> - Unity Learn: [Performance Optimization](https://learn.unity.com/tutorial/performance-optimization)

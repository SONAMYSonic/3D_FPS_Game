/// <summary>
/// 비밀번호 해시 처리를 위한 인터페이스입니다.
/// 
/// ───────────────────────────────────────────────────────────
/// 📚 학습 포인트: 인터페이스 분리 원칙 (ISP) & 의존성 역전 원칙 (DIP)
/// ───────────────────────────────────────────────────────────
/// 
/// 왜 인터페이스를 만들까요?
/// 
/// 비유: "충전기 규격"
/// - USB-C 규격(인터페이스)만 맞으면 어떤 충전기든 사용 가능
/// - 삼성 충전기, 애플 충전기 등 구현체가 바뀌어도 괜찮음
/// 
/// 코드에서도 마찬가지:
/// - IPasswordHasher 인터페이스만 맞추면 어떤 해시 알고리즘이든 사용 가능
/// - SHA256, bcrypt, Argon2 등 나중에 교체하기 쉬움
/// 
/// ───────────────────────────────────────────────────────────
/// </summary>
public interface IPasswordHasher
{
    /// <summary>
    /// 비밀번호를 해시화합니다.
    /// </summary>
    /// <param name="password">원본 비밀번호 (평문)</param>
    /// <returns>해시된 비밀번호 (Salt 포함)</returns>
    string HashPassword(string password);

    /// <summary>
    /// 입력된 비밀번호가 저장된 해시와 일치하는지 검증합니다.
    /// </summary>
    /// <param name="password">사용자가 입력한 비밀번호</param>
    /// <param name="storedHash">저장되어 있는 해시값</param>
    /// <returns>일치 여부</returns>
    bool VerifyPassword(string password, string storedHash);
}
